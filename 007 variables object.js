let fridge = {
    name: "Indesit",
    height: 180,
    width: 80,
    deep: 60,
    weight: 60,
    class: "A++"
};

console.log(fridge.name, fridge.weight);
console.log(fridge['name'], fridge['weight']);
