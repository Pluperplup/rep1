let a = 3;
let b = 3;

    if ((a - b) > 0){
        console.log(`${a} bigger than ${b}`);
    }else if((a - b) < 0) {
        console.log(`${b} bigger than ${a}`);
    } else {
        console.log(`${a} equal ${b}`);
    }
